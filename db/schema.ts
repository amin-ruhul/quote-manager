/*
 * Drizzle schema — the single source of truth for table shapes and inferred types.
 * Tables arrive with the phase that needs them (SPEC §8 / §14); Phase 0 only wires
 * up the connection and the migration pipeline.
 */

export {};
